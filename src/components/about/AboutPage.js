import React, { Component } from "react";
// import { Link } from "react-router";

export default class AboutPage extends Component {
  render () {
    return (
      <div>
        <h1>About</h1>
        <p>This application does stuff using react, redux, react router and some helpful libs built, of
          course, with webpack.</p>
      </div>
    );
  }
}
